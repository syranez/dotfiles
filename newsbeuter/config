always-display-description true
auto-reload yes
browser firefox
max-items 80
notify-format "%d new articles (%n unread articles, %f unread feeds)"
notify-program "/usr/bin/notify-send"
notify-screen yes
refresh-on-startup yes
reload-time 60
show-keymap-hint no
show-read-feeds no
show-read-articles no
suppress-first-reload yes

reset-unread-on-update "http://blog.fefe.de/rss.xml?html" 

#highlight feedlist "^.{4} N.*$" white blue bold
#highlight articlelist "^.{4} N.*$" white blue bold

# Tasten
unbind-key	C
unbind-key	A
unbind-key	n
unbind-key	p
unbind-key	N
unbind-key	k
unbind-key	l
unbind-key	q
bind-key	q	quit
bind-key	h	quit
bind-key	i	mark-feed-read
bind-key	j	next-unread
bind-key	ö	prev-unread
bind-key	n	toggle-article-read
bind-key	k	up
bind-key	l	down
bind-key	n	toggle-show-read-feeds

color background white blue
color listnormal white blue
color listfocus yellow blue bold
color info white blue bold
color article white blue bold
